---
- hosts: localhost
  gather_facts: no
  vars:
    state: present
    api_gw_spec_file: /Users/pjodouin/projects/ansible-api-gateway/samples/sample-swagger.yaml
    account: 123456789012

  tasks:
  - name: AWS API Gateway
    api_gw:
      state: "{{ state | default('present') }}"
      # api_id: '*'
      # api_id: 7vnky1q6oi
      api_id: 0avs2a68h5
#      deploy_api: True
      swagger_spec: "{{ api_gw_spec_file }}"

  - name: show results
    debug: var=api_gw_facts
